package parser;

usecasediagram SwimmyFish {
  @Player -- Play, ChangeProfilePic, Pay;
  @Server -- ShowAd, RegisterScore;

  CreditCard extends Pay;
  Bank extends Pay;
  ChangeProfilePic [premiumPlayer];
  CheckPremium;

  Pay --> CheckPremium;
  ShowAd --> Play [!premiumPlayer];
  RegisterScore --> Play [gameFinished];
}